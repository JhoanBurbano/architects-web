export enum TypeNotify {
  SUCCESS = "success",
  ERROR = "error",
  DEFAULT = "default",
  DARK = "dark",
  WARNING = "warning",
}

export enum Keys {
  USER = "USER",
  ROL = "ROL",
  TOKEN = "TOKEN",
}

export enum ROL {
  ADMIN = 'ADMIN',
  USER = 'USER',
  ASESOR = 'ASESOR',
  SECURITY = 'SECURITY',
}
